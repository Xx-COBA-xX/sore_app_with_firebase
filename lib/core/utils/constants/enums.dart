enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods { paypat, googtePay, applePay, visa, masterCard, creditCard, paystack, razorPay, paytm }
